select Makes.Make , 
count(ID) as CountNumberOfCars 
from VehicleDetails inner join Makes 
on Makes.MakeID = VehicleDetails.MakeID
where VehicleDetails.Year between 1950 and 2000
group by Makes.Make
order by CountNumberOfCars desc
